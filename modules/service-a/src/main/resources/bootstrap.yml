server:
  port: 8104
spring:
  application:
    name: service-a
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        namespace: xxgo
        group: xxgo
#        shared-configs[0]:
#          data-id: dev.properties
#          group: xxgo
#          refresh: false
        file-extension: yaml
#  profiles:
#    active: dev

#            discovery:
#                namespace: xxgo
#                group: xxgo
feign:
  hystrix:
    enabled: true
